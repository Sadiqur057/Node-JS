// Node JS => A server side environment , programming environment that can be used for building high scale applications that need to support multiple concurrent request. Run on server side

// modules => basic feature of functionality of programming language
// i) core modules => const fs = require('fs') => need to import
// ii) global modules => nodemon, globally with the system

// we can import a single function from any module
const fs = requrie('fs').writeFileSync // can be used like accessing property
